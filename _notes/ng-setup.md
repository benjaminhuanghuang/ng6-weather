

```
npm i -g typescript 
npm i -g @angular/cli

ng new Minimus --routing   # for the app pagination and routing

ng serve -o                # start server and open browser

ng g c home                # create component
```